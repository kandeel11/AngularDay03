import { ThemeChange } from './theme-change';

describe('ThemeChange', () => {
  it('should create an instance', () => {
    const directive = new ThemeChange();
    expect(directive).toBeTruthy();
  });
});
