import { FocusContent } from './focus-content';

describe('FocusContent', () => {
  it('should create an instance', () => {
    const directive = new FocusContent();
    expect(directive).toBeTruthy();
  });
});
