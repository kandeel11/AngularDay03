import { ScaleImage } from './scale-image';

describe('ScaleImage', () => {
  it('should create an instance', () => {
    const directive = new ScaleImage();
    expect(directive).toBeTruthy();
  });
});
