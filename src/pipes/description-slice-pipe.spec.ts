import { DescriptionSlicePipe } from './description-slice-pipe';

describe('DescriptionSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
